# ─── Database Connection ──────────────────────────────────────────────────────
# LOCAL development:
DATABASE_URL="postgresql://user:password@localhost:5432/flypatterndb?schema=public"
DIRECT_URL="postgresql://user:password@localhost:5432/flypatterndb?schema=public"
#
# SUPABASE / Vercel production (set in Vercel environment variables):
# DATABASE_URL — Supavisor pooler, port 6543 (MUST include ?pgbouncer=true):
#   postgresql://postgres.[REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&connect_timeout=30&pool_timeout=30
# DIRECT_URL — Direct connection, port 5432 (for migrations):
#   postgresql://postgres.[REF]:[PASSWORD]@aws-0-[REGION].supabase.com:5432/postgres

# Application URL (used for SEO metadata)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Application name
NEXT_PUBLIC_APP_NAME="FlyPatternDB"

# ─── OAuth Configuration ─────────────────────────────────────────────────────
# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# ─── Pipeline Configuration ──────────────────────────────────────────────────

# YouTube Data API v3 key (required for pipeline:discover)
# Get one at: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=""

# Anthropic API key (required for pipeline:extract)
# Get one at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=""

# Pipeline concurrency (optional, default: 5)
PIPELINE_CONCURRENCY="5"

# Auto-approve confidence threshold (optional, default: 0.7)
# Extractions above this threshold are auto-approved during pipeline:auto-approve
PIPELINE_CONFIDENCE_THRESHOLD="0.7"
